<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Diary</title>
    <link rel="stylesheet" href="../css/partials/header.css">
    <link
      href="https://fonts.googleapis.com/css2?family=Karla:wght@350;400;600;700;800&display=swap"
      rel="stylesheet"
    />
</head>
<body>
    <%- include("../partials/header") %> 
    <%- include("../partials/flash") %> 
    <h1>Food Diary</h1>
    <div>
        <!-- Change this to a date picker, add event listener to change href via JS -->
        <select name="date" id="date">
            <% for(diary of foodDiary) { %> 
                <option value="<%= diary.date %>"><%= diary.date %></option>
            <% } %> 
        </select>
    </div>
    <script src="/js/foodDiary/show.js"></script>
</body>
</html>