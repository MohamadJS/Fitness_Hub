<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipes</title>
    <link rel="icon" href="/icons/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="../css/partials/header.css">
    <link
      href="https://fonts.googleapis.com/css2?family=Karla:wght@350;400;600;700;800&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/recipe/index.css">
</head>
<body class="preload">
    <%- include("../partials/header") %> 
    <%- include("../partials/flash") %> 
    <main>
        <div class="container-main">
                <h1 class="title">Recipes</h1>
                <% if(currentUser) { %> 
                <a href="/recipes/new">Post a Recipe!</a>
                <% } %> 
            <div class="post">
                    <% for (let recipe of recipes) {%>
                        <figure class="recipe">
                                <img class="thumbnail" src="<%= recipe.image.thumbnail %>" alt="">
                                <div class="recipe-title-div">
                                    <h2 class="recipe-title"><%= recipe.title %></h2>
                                </div>
                                <a class="link" href="/recipes/<%= recipe._id %>">View Recipe</a>
                        </figure>
                    <% } %>
                    <div class="recipe-spacer"></div>
            </div>    
        </div>
    </main>
    <script src="/js/animation.js"></script>
</body>
</html>